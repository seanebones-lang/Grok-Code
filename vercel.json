{
  "buildCommand": "rm -f ~/.npmrc && npm install --legacy-peer-deps --ignore-scripts && prisma generate && next build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "rm -f ~/.npmrc .npmrc && npm install --legacy-peer-deps --ignore-scripts",
  "regions": ["iad1"],
  "env": {
    "PRISMA_GENERATE_DATAPROXY": "true"
  }
}
