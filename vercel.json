{
  "buildCommand": "rm -f ~/.npmrc && npm install --legacy-peer-deps --ignore-scripts && prisma generate && next build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "npm config set //registry.npmjs.org/:_authToken '' && npm config delete //registry.npmjs.org/:_authToken 2>/dev/null; npm install --legacy-peer-deps",
  "regions": ["iad1"],
  "env": {
    "PRISMA_GENERATE_DATAPROXY": "true"
  }
}
