from fastapi import FastAPI, WebSocket, WebSocketDisconnect; # ... existing; @app.websocket('/live') async def websocket_endpoint(websocket: WebSocket): await websocket.accept(); try: while True: data = await websocket.receive_text(); await websocket.send_text(f'Hyper-swarm: {data}'); except WebSocketDisconnect: pass; # rest unchanged