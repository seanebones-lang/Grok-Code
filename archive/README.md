# Archive — Superseded / Dead Code

This folder contains snapshots of code that is **not** the canonical system. Kept for reference only.

## Contents

| Folder | Source | Date | Description |
|--------|--------|------|-------------|
| `dead-code-parent-83ff171/` | Parent repo `/Grok-Code/src/` | 2026-02-26 | Parent repo state at commit 83ff171. Used for Vercel deployment; caused drift and breakage. **Do not use.** |

## Why This Exists

The parent repo (`Grok-Code`) was used for Vercel deployment. During deploy attempts, changes were made that broke the local system. The canonical system lives in this directory (`Grok-Code/Grok-Code`). This archive preserves the parent's superseded code so it can be referenced if needed — but it must never replace or be merged into the canonical system.
