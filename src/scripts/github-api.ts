// GitHub API stub for chat routes
export const getRepoTree = async (owner: string, repo: string, branch = 'main') => {
  return { tree: [] } // Stub response
}

export const getFileContent = async (owner: string, repo: string, path: string) => {
  return { content: '' }
}
