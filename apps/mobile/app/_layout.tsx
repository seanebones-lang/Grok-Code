import { ClerkProvider } from '@clerk/clerk-expo'; import { TamaguiProvider, createTamagui } from 'tamagui'; import { Stack } from 'expo-router'; import config from '../../libs/shared-ui/tamagui.config'; const tamaguiConfig = createTamagui(config); export default function RootLayout() { return ( <tamaguiConfig.Provider> <ClerkProvider publishableKey={process.env.EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY!}> <Stack screenOptions={{ headerShown: false, contentStyle: { backgroundColor: 'white' } }} /> </ClerkProvider> </tamaguiConfig.Provider> ); }