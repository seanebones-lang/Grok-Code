---
description: This directory is the ONLY canonical version of the system. Do not create variants or sync from parent.
alwaysApply: true
---

# Canonical System — Do Not Break

This codebase at `Grok-Code/Grok-Code` is the **only** version of the system. It runs locally at http://localhost:3000.

## Rules

1. **No alternate versions** — Do not create deployment variants, forks, or "improved" structures. This is the system.
2. **No parent sync** — The parent repo (`Grok-Code`) is superseded. Do not merge, copy, or sync from it. Its code is archived in `archive/dead-code-parent-83ff171/`.
3. **Run locally** — `cd Grok-Code/Grok-Code && npm run dev`
4. **Restore if drifted** — `git checkout canonical-2026-02-26` or `git reset --hard 37bd6e8`

Read `CANONICAL_SYSTEM.md` for full documentation.
