NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Copy to .env.local and fill with your Supabase project values
# Create project at supabase.com, add table: CREATE TABLE todos (id BIGSERIAL PRIMARY KEY, text TEXT NOT NULL, done BOOLEAN DEFAULT false, created_at TIMESTAMP DEFAULT NOW());
# For Vercel: add env vars in dashboard.