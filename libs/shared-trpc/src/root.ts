import { router } from '../trpc'; import { z } from 'zod'; import { users, eq } from '../../shared-db/src/schema'; // Assume db export const t = /* from trpc.ts */; export const appRouter = router({ hello: t.procedure.input(z.object({ name: z.string() })).query(({ input }) => `Hello Enterprise Factory, ${input.name}!`), getUser: t.procedure.input(z.string()).query(async ({ input: clerkId }) => ({ role: 'admin', clerkId })), }); export type AppRouter = typeof appRouter;