version: '3.8'
services:
  swarm:
    build: .
    context: .
    dockerfile: Dockerfile.swarm
    ports:
      - '8000:8000'
    volumes:
      - .:/app
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
  db:
    image: postgres:16
    # Prisma compat
